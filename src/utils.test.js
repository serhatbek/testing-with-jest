import { describe, expect, it } from "vitest";

describe("⭐ utils", () => {
  it("check 1 equal 1", () => {
    expect(1).toEqual(1);
  });
});

describe("⭐ utils", () => {
  it("check 2 equal 4", () => {
    expect(2 * 2).toEqual(4);
  });
});
